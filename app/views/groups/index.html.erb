<h1>Groups</h1>
<%= link_to 'Back', user_url(@user) %>

<br>

<% if !@groups.empty? %>
  <% @groups.each do |group| %>
    <%= link_to group.name, group_url(group) %>
  <% end %>
<% else %>
  <p>You are not a member of any groups yet.</p>
<% end %>
<br><br>
<%= link_to "Create a group", new_group_url %>